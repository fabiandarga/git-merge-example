const some = "11";
const other = 5;

function subtraction(a, b) {
    const number_a = parseInt(a)
    return number_a - b;
}

console.log( subtraction(some, other) );
console.log( subtraction(some, other) );
